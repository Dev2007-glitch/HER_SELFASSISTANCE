/**
 * Toast Notification Component
 * Disabled / muted per user request to prevent disturbing notifications.
 */

export function showToast(message, icon = '✨', duration = 3200) {
  // Silent no-op: notifications removed per user preference
  return;
}
